server:
  port: 9001
spring:
  application:
    name: goods
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

  datasource:
    url: jdbc:mysql://localhost:3306/shop_cloud_goods?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    database: 0
    host: localhost
    port: 6379
    password:

mybatis-plus:
  #  mapper文件
  mapper-locations: classpath:org/example/goods/mapper/*.xml
  #  实体类
  type-aliases-package: org.example.goods.model