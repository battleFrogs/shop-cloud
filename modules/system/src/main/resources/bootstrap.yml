server:
  port: 9002
spring:
  application:
    name: system
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

  datasource:
    url: jdbc:mysql://localhost:3306/shop_cloud_system?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    database: 0
    host: localhost
    port: 6379
    password:

mybatis-plus:
  #  mapper文件
  mapper-locations: classpath:org/example/system/mapper/*.xml
  #  实体类
  type-aliases-package: org.example.system.model